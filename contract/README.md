# Contract policy

Root `/contract` remains the single source for cross-repository Proto/OpenAPI contracts. This
repository does not maintain a parallel hand-written DTO tree. Generated gateway consumers must be
updated from a reviewed Root commit and checked in only when the repository's generation policy
requires it.
